{{- define "client_signature" -}}
{{- goName .OperationID false -}} (res interface{},
    {{- range $i, $arg := .Parameters}}
        {{- $arg.ToCamelCase }} {{ $arg.RenderType}} {{- if lt (inc $i) (len $.Parameters) -}}, {{- end -}}
    {{- end -}}
) (*http.Response, error)
{{- end}}
